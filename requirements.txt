# Core (only needed for REAL models; the FakeLLM offline demo needs none of these)
litellm>=1.40.0
python-dotenv>=1.0.0

# Optional: nicer online fetching (HARNESS_OFFLINE=0)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Applied bench on real datasets (experiments/applied): FRAMES loads via HuggingFace
# datasets; LoCoMo reads a local JSON. Not needed for the offline `harness.compare` demo.
datasets>=2.18.0

# Dev
pytest>=8.0.0
